{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos oficiales AXIOM VAULT",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "opener:default",
    "dialog:default",
    "fs:default",
    "fs:read-all",
    "fs:write-all"
  ]
}