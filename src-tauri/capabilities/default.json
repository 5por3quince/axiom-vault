{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "default",
  "description": "Permisos por defecto para AXIOM VAULT",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "opener:default",
    "fs:default",
    "dialog:default"
  ]
}